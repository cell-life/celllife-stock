package org.celllife.stock.domain.stock;

/**
 * The different types of stock in the system
 * ORDER = stock takes
 * RECEIVED = stock arrival at the facility (clinic)
 */
public enum StockType {

	ORDER,
	RECEIVED

}
